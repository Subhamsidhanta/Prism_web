# Prism frontend production environment example
# Copy to .env.production or set in Vercel project settings.

# Base URL of the deployed backend (Render) - no trailing slash.
VITE_API_BASE=https://YOUR_RENDER_BACKEND_HOST
